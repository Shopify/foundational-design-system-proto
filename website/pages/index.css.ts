import {style} from '@vanilla-extract/css';

import {vars} from '../components/theme.css';

export const placeholder1 = style({
  backgroundColor: vars.color.placeholder1,
});

export const placeholder2 = style({
  backgroundColor: vars.color.placeholder2,
});

export const newPlaceholder = style({
  backgroundColor: vars.color.newPlaceholder,
});
