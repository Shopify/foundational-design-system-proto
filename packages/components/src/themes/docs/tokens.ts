import {
  fonts,
  fontSizes,
  fontWeights,
  spacing,
  shadows,
  transitions,
} from '@polaris/tokens';

const docsTokens = {
  fonts,
  fontSizes,
  fontWeights,
  spacing,
  shadows,
  transitions,
};

export default docsTokens;
