export {assignRef} from './assignRef';

export {default as mapToProperty} from './mapToProperty';

export {isValidElementWithRef} from './isValidElementWithRef';

export type {AnyRef} from './types';

export const isBrowser = typeof window !== 'undefined';
