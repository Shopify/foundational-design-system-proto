import {docsTheme} from '@polaris/components';

const config = {
  tokens: {
    ...docsTheme.tokens,
    spacing: {
      ...docsTheme.tokens.spacing,
      4: '4rem',
    },
  },
};

export default config;
